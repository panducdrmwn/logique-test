## Setup instructions

Install the dependencies and devDependencies and start the server.

```sh
cd logique-test
npm i
npm run dev
```

## Aprroach Overview
```sh
i use materialUI as my styling library.
i use react useContext hook to manage state between cart and product components.
the shopping cart uses localstorage to store data to achieve the state persist on every page refresh.
```
